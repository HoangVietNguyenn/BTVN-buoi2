# Tạo một danh sách ví dụ
my_list = [10, 20, 30, 40, 50]

# Sử dụng phương thức index() để lấy phần tử tại chỉ số 3
# chỉ số chạy bắt đầu từ 0
element_at_index = my_list.index(40)

# In ra giá trị của phần tử tại chỉ số 
print("Phần tử tại chỉ số :", element_at_index)
